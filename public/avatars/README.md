Drop BD headshots here to put their face on the map.

Name each file by the user id used in src/lib/users.ts:
  praveen.png, nabil.png, snehal.png, dinit.png

A square photo works best; it renders as a 38px circular chip.
If a file is missing, the map falls back to an initials chip automatically.
